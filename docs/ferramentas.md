# Ferramentas

Portal da Transparência: Seguro Defeso expõe 1 ferramenta (todas somente leitura).

### 1. `portal_transparencia_seguro_consultar`
**Input**: `cpf` (opcional), `nis` (opcional), `data_inicio` (opcional), `data_fim` (opcional)

Portal da Transparência: Seguro Defeso, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Portal da Transparência: Seguro Defeso
```
